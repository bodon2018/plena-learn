"use client";

import AccountScreen from "@/features/account/AccountScreen";

/**
 * User Account page.
 * Displays profile, settings, and login/logout options.
 */
export default function AccountPage() {
  return <AccountScreen />;
}