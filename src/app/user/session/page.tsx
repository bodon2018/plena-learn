"use client";

import SessionScreen from "@/features/session/SessionScreen";

/**
 * User "Session" route.
 * Renders the recording screen where athletes/coaches
 * can capture a new practice or game.
 */
export default function SessionPage() {
  return <SessionScreen />;
}
